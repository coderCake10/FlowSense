# Draco decoder

Decoder for Draco-compressed building models, served from this app so the
kiosk works without internet access. Copied unchanged from the `three`
package (`three/examples/jsm/libs/draco/gltf/`, three 0.185.1; Draco is
Apache-2.0 licensed). `npm run models:check` reports if these files go missing.
